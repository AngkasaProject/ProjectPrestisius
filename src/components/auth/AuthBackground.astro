<div class="absolute inset-0">
  <!-- Gradient -->
  <div class="absolute inset-0 bg-gradient-to-b from-white via-zinc-50 to-white"></div>

  <!-- Glow -->
  <div class="absolute left-1/2 top-1/2 h-[550px] w-[550px] -translate-x-1/2 -translate-y-1/2 rounded-full bg-lime-400/20 blur-3xl"></div>

  <!-- Grid -->
  <div
    class="absolute inset-0 opacity-[0.03]"
    style="
      background-image:
        linear-gradient(to right,#000 1px,transparent 1px),
        linear-gradient(to bottom,#000 1px,transparent 1px);
      background-size:42px 42px;
    "
  ></div>
</div>